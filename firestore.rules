rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // フィードバックは誰でも書き込み可（読み取りは不可）
    match /feedbacks/{feedbackId} {
      allow create: if true;
      allow read, update, delete: if false;
    }
  }
}
